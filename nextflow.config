manifest {
    homePage = 'https://github.com/LanguageMachines/PICCL'
    description = 'PICCL'
    mainScript = 'ticcl.nf'
}

process.$indexer.cpus = executor.cpus
process.$resolver.cpus = executor.cpus
process.$rank.cpus = executor.cpus
process.$foliacorrect.cpus = executor.cpus

